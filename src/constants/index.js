const PRODUCT_STATUS= Object.freeze({
    AVAILABLE: 'available',
    OUT_OF_STOCK:'out_of_stock',
});

const USER_ROLES= Object.freeze({
    ADMIN: 'admin',
    USER:'user',
});

module.exports={
    PRODUCT_STATUS,
    USER_ROLES,
}